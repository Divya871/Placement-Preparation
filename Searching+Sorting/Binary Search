int binarysearch(vector<int>&arr,int data)
{
    int si=0,ei=n-1,mid=0;
    while(si<=ei)
    {
        int mid=(si+ei)/2;
        if(arr[mid]>data) 
          {ei=mid-1;}
        else if(arr[mid]<data)
          {si=mid+1;}
        else 
          {return arr[mid];}
        
    }
}
