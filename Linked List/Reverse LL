 ListNode* reverseList(ListNode* head) {
        ListNode*prev=NULL,*curr=head,*n;
        while(curr!=NULL)
        {
            n=curr->next;
            curr->next=prev;
            prev=curr;
            curr=n;
        }
        head=prev;
        return head;
    }
